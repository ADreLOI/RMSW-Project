% Drafting owner: Andrea. Keep scientific content student-authored.
\subsection{Data, Materials, Participants, or Other Sources}

The evidence will come from simulation; no human participants are involved. Two identical USVs will survey a known rectangular grid. A policy-independent, temporary speed reduction of one USV will create unequal progress, motivating online reassignment of unfinished cells. Its onset and duration will be generated before the paired policy runs. The slowed USV observes its own progress; its peer learns that progress through timestamped position, coverage, and task-claim messages over a direct boat-to-boat radio link. The synthetic link model will combine distance-dependent signal strength with a two-state, temporally correlated fade process; recent signal and position measurements may give warning before some outages.

A forecast will be positive when an outage of at least two consecutive missed scheduled exchanges is predicted to begin within the next three update intervals. The predictor will use only recent received-signal measurements and last known relative positions, never future link states. A simple forecast extrapolates recent signal strength and relative motion over the next three exchanges, warning when two consecutive projected signals fall below the reception threshold; its trend window and warning margin will be tuned on separate pilot seeds and frozen before evaluation. Across conditions, we will vary the fade state's persistence to set its target mean episode duration (in update intervals), while holding the long-run fade fraction and distance-loss rule fixed. Longer fades necessarily imply fewer fade onsets. Each policy's realized outage duration, outage frequency, and link availability will be logged, since its route can change the actual link history. Mission completion is the first time all survey cells have been observed, excluding return or data upload. Redundant coverage counts survey observations of cells already observed once. A common timeout will be fixed before evaluation.

Each run will record true and locally known coverage, positions, link and message events, predictions, task claims, completion or timeout, travel distance, and bytes sent. Configurations, random seeds, software versions, and any reused code revision will be retained. Parameters will be source-based where possible; synthetic assumptions and their units will be explicit. The grid, idealized sensing, and synthetic link limit conclusions about real vessels and seabed-map quality.
